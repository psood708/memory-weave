# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
docs/
*.egg-info

# Virtual environments
.venv

# Test + tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
*.log

# TypeScript build artifacts
tsconfig.tsbuildinfo
frontend/tsconfig.tsbuildinfo

# Secrets — never commit (root-anchored so frontend/.env.* can be committed)
/.env
/.env.development
/.env.production

# Runtime state — env-namespaced data dirs replace these
.chroma/
.claude
kg_store.json
data/

# Personal — scratch files, notes, interview prep (yours only, never commits)
PERSONAL.md
PROJECT_STATUS.md
scripts/
scratch/
notes/
