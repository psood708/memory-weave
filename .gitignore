# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
docs/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit (root-anchored so frontend/.env.* can be committed)
/.env
/.env.development
/.env.production

# Runtime state — env-namespaced data dirs replace these
.chroma/
.claude
kg_store.json
data/
